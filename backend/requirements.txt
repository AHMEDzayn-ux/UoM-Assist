# CPU-only PyTorch build (the default PyPI wheel bundles CUDA libs and is
# several times larger/heavier in memory - not needed on Render's CPU-only dynos,
# and was pushing the free-tier 512MB limit).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.1

# Core RAG dependencies
langchain>=0.1.0
langchain-community>=0.0.13
langchain-groq>=1.1.0
langchain-google-genai>=2.0.0  # Gemini provider

# Vector database
faiss-cpu>=1.7.4

# Document processing
pypdf>=3.17.4
pdfplumber>=0.11.0  # table-aware PDF extraction (primary path; pypdf is the fallback)

# Embeddings
sentence-transformers>=2.3.1

# Tokenization (for production-grade chunking)
tiktoken>=0.5.2

# Advanced Retrieval Optimization
rank-bm25>=0.2.2  # BM25 keyword search for hybrid retrieval

# FastAPI and server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0.25

# Auth (used by services/auth_service.py - previously undeclared, installed
# transitively/manually in local venv only, which is why Render's clean
# install crashed on import)
bcrypt>=4.1.2
PyJWT>=2.8.0

# Voice / TTS (lazy-imported in services/tts_service.py; without it the app
# silently falls back to browser TTS instead of crashing, but this makes it
# actually work in production)
edge-tts>=7.0.0

# Testing
pytest>=7.4.4
pytest-cov>=4.1.0
pytest-asyncio>=0.23.3
httpx>=0.26.0

# Utilities
pydantic>=2.5.3
pydantic-settings>=2.1.0

# WhatsApp Integration
requests>=2.31.0
